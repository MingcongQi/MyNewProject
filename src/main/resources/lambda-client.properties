# AWS Lambda Client Configuration
# Replace with your actual Lambda function URL
lambda.function.url=https://your-lambda-function-url.lambda-url.us-east-1.on.aws/
lambda.region=us-east-1
lambda.timeout.seconds=30

# Logging configuration
logging.level=INFO
